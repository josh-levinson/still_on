[build]
builder = "nixpacks"

[deploy]
preDeployCommand = "bundle exec rails assets:precompile"
startCommand = "bundle exec rails db:prepare && bundle exec rails db:migrate && bundle exec thrust rails server -b 0.0.0.0"
healthcheckPath = "/up"
