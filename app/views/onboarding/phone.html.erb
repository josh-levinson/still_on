<div data-controller="phone-input">
  <p class="ob-label">Get started</p>
  <div class="onboarding-card-header">
    <h2>What's your number?</h2>
    <p>We'll send a quick code. No spam &mdash; just your hangout reminders.</p>
  </div>

  <% if flash[:error].present? %>
    <div class="onboarding-error"><%= flash[:error] %></div>
  <% end %>

  <%= form_with url: onboarding_submit_phone_path, method: :post, class: "onboarding-form" do |f| %>
    <%= f.telephone_field :phone,
          placeholder: "(555) 000-0000",
          inputmode: "tel",
          autocomplete: "tel-national",
          class: "onboarding-input phone-field",
          data: { action: "input->phone-input#format", phone_input_target: "display" },
          required: true,
          autofocus: true %>

    <p class="ob-annotation">No password needed</p>

    <%= f.submit "Send code \u2192", class: "btn-onboarding-primary" %>
  <% end %>
</div>
